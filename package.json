{
  "name": "stripe-billing-backend",
  "version": "1.0.0",
  "description": "Subscription billing backend built on Stripe's Payment Intents + Webhooks API, with idempotency-key handling and failure-resilient webhook processing.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.1",
    "stripe": "^16.2.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.4",
    "supertest": "^7.0.0"
  }
}
